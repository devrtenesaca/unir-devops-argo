apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-docker
spec:
  #replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge: 1
       maxUnavailable: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: flask-docker
  template:
    metadata:
      labels:
        app: flask-docker
    spec:
      containers:
        - image: docker.io/anietounir/flask-docker:3.0
          name: flask-docker
          ports:
            - containerPort: 5000
           resources:
          requests:
            cpu: "100m"  # Baseline for HPA
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
